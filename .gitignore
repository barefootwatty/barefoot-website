assets/raw/
*.bak
.DS_Store

# Generated media pages are rebuilt by tools/build_media.py — but they ARE
# published, so they stay tracked. (Nothing to ignore here; note kept for
# whoever wonders why site/media/ is committed rather than built on deploy:
# Vercel serves this repo as-is, there is no build step.)
